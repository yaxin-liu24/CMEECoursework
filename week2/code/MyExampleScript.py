def foo(x):
    """
    This function takes an integer `x`, squares it, and prints the squared result.
    Note:
    This function only performs the squaring operation and outputs the result, 
    but does not return any value for further use.
    """
    x *= x  # This is equivalent to x = x * x
    print(x)

foo(2)

